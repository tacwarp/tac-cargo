{2 items
"file*path":"./MIGRATION_PLAN.md"
"content":"# TAC Cargo - Migration & Component Upgrade Plan **Version:** 1.0 **Target Completion:** 6-8 Weeks **Risk Level:** Medium **Last Updated:** January 2025 --- ## Table of Contents 1. [Overview](#overview) 2. [Migration Strategy](#migration-strategy) 3. [Component-by-Component Migration](#component-by-component-migration) 4. [Data Migration](#data-migration) 5. [Testing Strategy](#testing-strategy) 6. [Rollback Plan](#rollback-plan) 7. [Post-Migration Checklist](#post-migration-checklist) --- ## Overview ### Current State TAC Cargo is currently a **frontend prototype** with: - ✅ Excellent UI components (95% complete) - ✅ Type definitions (85% complete) - ⚠️ Mock data throughout the application - ❌ No database connection (0% backend) - ❌ No authentication enforcement - ❌ No API implementation ### Target State Transform into a **production-ready SaaS platform** with: - ✅ Full database integration (PostgreSQL via Supabase) - ✅ Working authentication and authorization - ✅ RESTful API endpoints - ✅ Real-time data updates - ✅ PDF generation - ✅ Barcode scanning - ✅ Test coverage >70% ### Migration Approach **Incremental Migration Strategy:** - Minimize disruption to existing UI - Add backend progressively - Test each component after migration - Maintain backward compatibility during transition - Feature flags for gradual rollout --- ## Migration Strategy ### Phase 1: Foundation (Week 1) **Goals:** - Deploy database - Set up authentication - Create query/mutation layer - Establish testing framework **Deliverables:** - Supabase project with complete schema - Authentication middleware - Query functions for all entities - Initial test suite **Success Criteria:** - Database deployed and accessible - Can create/read/update/delete records via Supabase client - Tests passing (>50% coverage) ### Phase 2: Core APIs (Week 2) **Goals:** - Implement shipment APIs - Implement customer APIs - Add validation layer - Connect one page to real data **Deliverables:** - `/api/shipments/*`endpoints -`/api/customers/_`endpoints - Zod validation schemas - Dashboard page with real metrics **Success Criteria:** - API endpoints return real data - Validation catches invalid inputs - Dashboard shows actual database stats ### Phase 3: Feature Migration (Weeks 3-5) **Goals:** - Migrate all pages to real data - Implement PDF generation - Add barcode functionality - Build scanning system **Deliverables:** - All pages connected to database - Invoice PDF generation - Barcode generation/scanning - Real-time updates **Success Criteria:** - No mock data remaining - Can generate and download invoice PDFs - Can scan barcodes to update shipments - Realtime subscriptions working ### Phase 4: Testing & Polish (Week 6) **Goals:** - Achieve >70% test coverage - Performance optimization - Security hardening - Documentation **Deliverables:** - Complete test suite - CI/CD pipeline - Security audit report - Deployment documentation **Success Criteria:** - All tests passing - Lighthouse score >90 - Security scan clean - Production deployment successful --- ## Component-by-Component Migration ### 1. Dashboard Page Migration **Current State:**`app/(dashboard)/dashboard/page.tsx`- Mock KPI data - Hardcoded charts - No database connection **Migration Steps:** 1. **Create metrics query function**`typescript // lib/supabase/queries/metrics.ts export async function getDashboardMetrics() { // Implementation provided in IMPLEMENTATION_GUIDE.md } `2. **Update page to use Server Components**`typescript // app/(dashboard)/dashboard/page.tsx import { getDashboardMetrics } from '@/lib/supabase/queries/metrics' export default async function DashboardPage() { const metrics = await getDashboardMetrics() // Render with real data } `3. **Add error handling**`typescript import { Suspense } from 'react' import { ErrorBoundary } from '@/components/error-boundary' export default function DashboardPage() { return ( <ErrorBoundary fallback={<DashboardError />}> <Suspense fallback={<DashboardSkeleton />}> <DashboardContent /> </Suspense> </ErrorBoundary> ) } `4. **Test migration**`bash # Run development server npm run dev # Navigate to /dashboard # Verify metrics load correctly # Check for errors in console `**Estimated Time:** 1 day **Dependencies:** Database deployed, metrics query created **Risk Level:** Low --- ### 2. Shipments Page Migration **Current State:**`app/(dashboard)/dashboard/shipments/page.tsx`- Mock shipment data - Non-functional filters - No create/update/delete operations **Migration Steps:** 1. **Convert to Client Component with React Query**`typescript 'use client' import { useShipments } from '@/lib/hooks/use-shipments' export default function ShipmentsPage() { const { data, isLoading, error } = useShipments() if (isLoading) return <ShipmentsTableSkeleton /> if (error) return <ErrorDisplay error={error} /> return <ShipmentsTable data={data} /> } `2. **Implement filters**`typescript const [filters, setFilters] = useState({ status: '', search: '', }) const { data } = useShipments(filters) `3. **Add create modal**`typescript import { ShipmentForm } from '@/components/dashboard/shipment-form' const [showCreateModal, setShowCreateModal] = useState(false) `4. **Implement bulk actions**`typescript const [selectedIds, setSelectedIds] = useState<string[]>([]) const handleBulkDelete = async () => { await Promise.all( selectedIds.map(id => deleteShipment(id)) ) refetch() } `5. **Test migration** - Create new shipment - Edit existing shipment - Delete shipment - Test filters - Test bulk actions **Estimated Time:** 2-3 days **Dependencies:** Shipments API, React Query setup **Risk Level:** Medium **Rollback:** Keep mock data version in separate file for fallback --- ### 3. Invoice Page Migration **Current State:**`app/(dashboard)/dashboard/invoices/page.tsx`- Mock invoice data - No PDF generation **Migration Steps:** 1. **Create invoice API endpoints**`typescript // app/api/invoices/route.ts // GET, POST endpoints `2. **Implement invoice creation form**`typescript import { InvoiceForm } from '@/components/dashboard/invoice-form' ` 3. **Add PDF generation** ``typescript const handleDownloadPDF = async (invoiceId: string) => { const response = await fetch(`/api/invoices/${invoiceId}/pdf`) const blob = await response.blob() // Create download link const url = window.URL.createObjectURL(blob) const a = document.createElement('a') a.href = url a.download = `invoice-${invoiceId}.pdf`a.click() } `` 4. **Connect to shipments** - Auto-populate from selected shipments - Calculate pricing automatically - Apply GST correctly 5. **Test migration** - Create invoice from shipments - Generate and download PDF - Verify GST calculations - Test email sending (if implemented) **Estimated Time:** 3-4 days **Dependencies:** Puppeteer setup, Invoice API **Risk Level:** Medium-High (PDF generation can be tricky) **Rollback:** Disable PDF generation, allow manual PDF upload --- ### 4. Scanning Page Migration **Current State:**`app/(dashboard)/dashboard/scanning/page.tsx`- Beautiful UI - Completely non-functional - No camera integration **Migration Steps:** 1. **Implement camera access**`typescript const videoRef = useRef<HTMLVideoElement>(null) const startCamera = async () => { const stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: 'environment' } }) videoRef.current.srcObject = stream } `2. **Add barcode scanning library**`bash npm install zxing-wasm ` `typescript import { BrowserMultiFormatReader } from '@zxing/library' const codeReader = new BrowserMultiFormatReader() const result = await codeReader.decodeOnceFromVideoDevice( undefined, videoRef.current ) `3. **Implement scan result handling**`typescript const handleScan = async (awbNumber: string) => { // Lookup shipment const shipment = await getShipmentByAwb(awbNumber) // Show in UI setScannedShipment(shipment) // Allow status update } `4. **Add batch scanning**`typescript const [scannedShipments, setScannedShipments] = useState<string[]>([]) const handleBatchUpdate = async (status: string) => { await Promise.all( scannedShipments.map(id => updateShipmentStatus(id, { status }) ) ) } `5. **Test migration** - Test camera access on mobile - Scan test barcodes - Verify shipment lookup - Test batch updates - Handle duplicates **Estimated Time:** 2-3 days **Dependencies:** Barcode generation, camera permissions **Risk Level:** High (camera/barcode scanning can be unreliable) **Rollback:** Manual AWB entry only, disable camera --- ### 5. Customer Management Migration **Current State:** Basic customer UI, no functionality **Migration Steps:** 1. **Create customer APIs**`typescript // app/api/customers/route.ts // GET, POST, PATCH, DELETE `2. **Build customer form**`typescript import { CustomerForm } from '@/components/dashboard/customer-form' `3. **Implement GSTIN validation**`typescript const validateGSTIN = (gstin: string) => { // Validate format and checksum } `4. **Add credit limit tracking**`typescript const checkCreditLimit = async (customerId: string, amount: number) => { const customer = await getCustomerById(customerId) const outstanding = await getOutstandingAmount(customerId) if (outstanding + amount > customer.credit_limit) { throw new Error('Credit limit exceeded') } } `5. **Test migration** - Create customer - Edit customer details - Validate GSTIN - Check credit limit enforcement - Link to shipments/invoices **Estimated Time:** 2 days **Dependencies:** Customer API **Risk Level:** Low-Medium --- ### 6. Manifest Management Migration **Current State:** Minimal implementation **Migration Steps:** 1. **Create manifest APIs**`typescript // app/api/manifests/route.ts `2. **Build manifest creation flow** - Select shipments - Assign vehicle/driver - Generate manifest number - Print manifest 3. **Add shipment assignment**`typescript const addShipmentToManifest = async ( manifestId: string, shipmentId: string ) => { await updateShipment(shipmentId, { manifest_id: manifestId, status: 'in_transit', }) } `4. **Implement manifest tracking** - Update departure/arrival times - Track overall status - Update all shipments when manifest completes 5. **Test migration** - Create manifest - Add shipments - Track progress - Complete manifest - Verify shipment updates **Estimated Time:** 2-3 days **Dependencies:** Shipment API **Risk Level:** Medium --- ### 7. Authentication Migration **Current State:**`middleware.ts`with basic redirects **Migration Steps:** 1. **Update middleware**`typescript // Enhanced version in IMPLEMENTATION_GUIDE.md `2. **Create login page**`typescript // app/login/page.tsx import { createClient } from '@/lib/supabase/client' const handleLogin = async (email: string, password: string) => { const supabase = createClient() const { error } = await supabase.auth.signInWithPassword({ email, password, }) } `3. **Implement signup flow**`typescript // app/signup/page.tsx const handleSignup = async (data: SignupData) => { // Create auth user const { data: authData } = await supabase.auth.signUp({ email: data.email, password: data.password, }) // Create user record await supabase.from('users').insert({ id: authData.user.id, full_name: data.full_name, organization_id: data.organization_id, }) } `4. **Add password reset** ``typescript // app/forgot-password/page.tsx const handleResetPassword = async (email: string) => { await supabase.auth.resetPasswordForEmail(email, { redirectTo:`${window.location.origin}/reset-password`, }) } `` 5. **Implement RBAC** ``typescript // lib/auth/permissions.ts export function hasPermission( user: User, action: string, resource: string ): boolean { const permissions = getRolePermissions(user.role) return permissions.includes(`${action}:${resource}`) } `` 6. **Test migration** - Login/logout - Signup flow - Password reset - Protected routes - Role-based access - Session persistence **Estimated Time:** 3-4 days **Dependencies:** Supabase Auth configured **Risk Level:** High (critical security component) **Rollback:** Keep development bypass flag --- ### 8. Real-Time Updates Migration **Current State:** Static data, manual refreshes **Migration Steps:** 1. **Set up Supabase Realtime** `typescript import { createClient } from '@/lib/supabase/client' useEffect(() => { const supabase = createClient() const subscription = supabase .channel('shipments') .on('postgres_changes', { event: '_', schema: 'public', table: 'shipments', }, (payload) => { // Update local state queryClient.invalidateQueries(['shipments']) }) .subscribe() return () => { subscription.unsubscribe() } }, []) `2. **Implement optimistic updates**`typescript const updateShipment = useMutation({ mutationFn: async (data) => { // API call }, onMutate: async (newData) => { // Cancel outgoing refetches await queryClient.cancelQueries(['shipment', id]) // Snapshot previous value const previous = queryClient.getQueryData(['shipment', id]) // Optimistically update queryClient.setQueryData(['shipment', id], newData) return { previous } }, onError: (err, newData, context) => { // Rollback on error queryClient.setQueryData(['shipment', id], context.previous) }, }) ` 3. **Add live notifications** ``typescript import { toast } from 'sonner' subscription.on('INSERT', (payload) => { toast.info(`New shipment created: ${payload.new.awb_number}`) }) `` 4. **Test migration** - Open two browser windows - Update shipment in one - Verify update appears in other - Test with slow network - Verify no duplicate updates **Estimated Time:** 2 days **Dependencies:** React Query setup **Risk Level:** Medium --- ## Data Migration ### Seed Data Creation **Objective:** Create realistic test data for development and staging **Steps:** 1. **Create seed script** ``typescript // scripts/seed.ts import { createClient } from '@supabase/supabase-js' const supabase = createClient( process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_KEY // Service key for admin access ) async function seed() { // Create organization const { data: org } = await supabase .from('organizations') .insert({ name: 'TAC Cargo', legal_name: 'TAC Cargo Private Limited', gstin: '10AABCT1234A1Z5', }) .select() .single() // Create users const users = [ { email: 'admin@taccargo.com', password: 'admin123', full_name: 'Admin User', role: 'admin', }, { email: 'operator@taccargo.com', password: 'operator123', full_name: 'Operator User', role: 'operator', }, ] for (const user of users) { // Create auth user const { data: authData } = await supabase.auth.admin.createUser({ email: user.email, password: user.password, email_confirm: true, }) // Create user record await supabase.from('users').insert({ id: authData.user.id, organization_id: org.id, email: user.email, full_name: user.full_name, role: user.role, }) } // Create customers const customers = [ { customer_code: 'CUST001', name: 'ABC Traders', email: 'abc@example.com', phone: '9876543210', billing_address: { line1: '123 Main St', city: 'Delhi', state: 'Delhi', pincode: '110001', }, }, // Add more customers ] const createdCustomers = await supabase .from('customers') .insert( customers.map(c => ({ ...c, organization_id: org.id })) ) .select() // Create shipments const shipments = [] for (let i = 0; i < 100; i++) { shipments.push({ organization_id: org.id, customer_id: createdCustomers[i % createdCustomers.length].id, awb_number: `AWB${Date.now()}${i}`, awb_date: new Date().toISOString().split('T')[0], origin: { line1: 'Imphal Office', city: 'Imphal', state: 'Manipur', pincode: '795001', }, destination: { line1: `Destination ${i}`, city: 'Delhi', state: 'Delhi', pincode: '110001', }, shipper: { name: 'Shipper Name', phone: '9876543210', }, consignee: { name: 'Consignee Name', phone: '9876543211', }, package_type: ['document', 'parcel', 'pallet'][i % 3], weight: Math.random() * 100, freight_charges: Math.random() * 5000, status: ['draft', 'in_transit', 'delivered'][i % 3], }) } await supabase.from('shipments').insert(shipments) console.log('Seed completed successfully') } seed().catch(console.error) `` 2. **Run seed script** `bash npx tsx scripts/seed.ts ` 3. **Verify data** `sql -- Run in Supabase SQL Editor SELECT (SELECT COUNT(*) FROM organizations) as orgs, (SELECT COUNT(*) FROM users) as users, (SELECT COUNT(*) FROM customers) as customers, (SELECT COUNT(*) FROM shipments) as shipments; ` **Estimated Time:** 1 day **Risk Level:** Low --- ### Production Data Migration **Scenario:** Migrating from another system (if applicable) **Steps:** 1. **Export data from old system** `bash # Export to CSV/JSON ` 2. **Transform data** `typescript // scripts/transform.ts import * as fs from 'fs' import * as csv from 'csv-parser' const results = [] fs.createReadStream('old-data.csv') .pipe(csv()) .on('data', (row) => { results.push({ // Transform to new schema awb_number: row.old_awb_field, // ... etc }) }) .on('end', () => { fs.writeFileSync('transformed.json', JSON.stringify(results)) }) ` 3. **Validate transformed data** `typescript import { ShipmentCreateSchema } from '@/lib/schemas/shipment' for (const record of transformedData) { try { ShipmentCreateSchema.parse(record) } catch (error) { console.error('Invalid record:', record, error) } } ` 4. **Import in batches** ``typescript const BATCH_SIZE = 1000 for (let i = 0; i < transformedData.length; i += BATCH_SIZE) { const batch = transformedData.slice(i, i + BATCH_SIZE) const { error } = await supabase .from('shipments') .insert(batch) if (error) { console.error('Batch failed:', i, error) // Log failed batch for retry } else { console.log(`Imported ${i + batch.length} records`) } } `` 5. **Verify import** `sql -- Compare counts SELECT COUNT(*) FROM shipments; -- Check data integrity SELECT * FROM shipments WHERE awb_number IS NULL; SELECT * FROM shipments WHERE total_amount < 0; ` **Estimated Time:** 3-5 days (depends on data volume) **Risk Level:** High (data loss risk) **Rollback:** Keep old system running in parallel for 1-2 weeks --- ## Testing Strategy ### Unit Testing **Target Coverage:** 80% **Priority Areas:** 1. Validation schemas (Zod) 2. Pricing calculations 3. GST calculations 4. Barcode generation 5. Rate limiting 6. Utility functions **Example Test:** `typescript // tests/unit/gst-calculation.test.ts import { describe, it, expect } from 'vitest' import { calculateGST } from '@/lib/utils/gst' describe('GST Calculation', () => { it('should calculate IGST for inter-state', () => { const result = calculateGST({ amount: 1000, fromState: 'Delhi', toState: 'Maharashtra', }) expect(result.igst).toBe(180) expect(result.cgst).toBe(0) expect(result.sgst).toBe(0) }) it('should calculate CGST+SGST for intra-state', () => { const result = calculateGST({ amount: 1000, fromState: 'Delhi', toState: 'Delhi', }) expect(result.igst).toBe(0) expect(result.cgst).toBe(90) expect(result.sgst).toBe(90) }) }) ` ### Integration Testing **Target Coverage:** Key user flows **Priority Flows:** 1. Create shipment → Generate invoice → Download PDF 2. Scan barcode → Update status → Verify event logged 3. Create customer → Create shipment → Link to invoice 4. Login → Create shipment → Logout → Login → View shipment **Example Test:** `typescript // tests/integration/shipment-to-invoice.test.ts import { describe, it, expect } from 'vitest' import { createClient } from '@/lib/supabase/server' describe('Shipment to Invoice Flow', () => { it('should create invoice from shipments', async () => { const supabase = createClient() // Create customer const { data: customer } = await supabase .from('customers') .insert({ name: 'Test Customer', ... }) .select() .single() // Create shipments const { data: shipments } = await supabase .from('shipments') .insert([ { customer_id: customer.id, freight_charges: 1000, ... }, { customer_id: customer.id, freight_charges: 2000, ... }, ]) .select() // Create invoice from shipments const { data: invoice } = await supabase .from('invoices') .insert({ customer_id: customer.id, subtotal: 3000, ... }) .select() .single() // Link invoice items await supabase.from('invoice_items').insert([ { invoice_id: invoice.id, shipment_id: shipments[0].id, ... }, { invoice_id: invoice.id, shipment_id: shipments[1].id, ... }, ]) // Verify expect(invoice.subtotal).toBe(3000) }) }) ` ### E2E Testing **Target Coverage:** Critical user journeys **Priority Journeys:** 1. Complete shipment creation flow 2. Invoice generation and PDF download 3. Barcode scanning 4. Dashboard navigation 5. Authentication flow **Example Test:** `typescript // tests/e2e/create-shipment.spec.ts import { test, expect } from '@playwright/test' test('should create shipment end-to-end', async ({ page }) => { // Login await page.goto('/login') await page.fill('[name="email"]', 'test@example.com') await page.fill('[name="password"]', 'password123') await page.click('button[type="submit"]') // Navigate to create shipment await page.goto('/dashboard/shipments/new') // Fill form await page.fill('[name="origin.city"]', 'Imphal') await page.fill('[name="destination.city"]', 'Delhi') await page.fill('[name="weight"]', '10') await page.fill('[name="freight_charges"]', '1000') // Submit await page.click('button[type="submit"]') // Verify success await expect(page.getByText('Shipment created')).toBeVisible() // Verify in list await page.goto('/dashboard/shipments') await expect(page.getByText('Imphal')).toBeVisible() }) ` ### Load Testing **Tool:** k6 **Scenarios:** 1. Dashboard load (100 concurrent users) 2. Shipment creation (50 req/sec) 3. Tracking API (1000 req/sec) 4. PDF generation (10 concurrent) **Example Test:** `javascript // tests/load/api-shipments.js import http from 'k6/http' import { check, sleep } from 'k6' export const options = { stages: [ { duration: '1m', target: 50 }, { duration: '3m', target: 50 }, { duration: '1m', target: 0 }, ], thresholds: { http_req_duration: ['p(95)<500'], http_req_failed: ['rate<0.01'], }, } export default function () { const res = http.get('https://your-domain.com/api/shipments') check(res, { 'status is 200': (r) => r.status === 200, 'response time < 500ms': (r) => r.timings.duration < 500, }) sleep(1) } ` **Run Load Test:** `bash k6 run tests/load/api-shipments.js ` --- ## Rollback Plan ### Component-Level Rollback **Strategy:** Feature flags for gradual rollout **Implementation:** `typescript // lib/config/features.ts export const FEATURES = { USE_REAL_DATA: process.env.NEXT_PUBLIC_USE_REAL_DATA === 'true', PDF_GENERATION: process.env.NEXT_PUBLIC_PDF_ENABLED === 'true', BARCODE_SCANNING: process.env.NEXT_PUBLIC_SCANNING_ENABLED === 'true', } // Usage in component import { FEATURES } from '@/lib/config/features' export default function ShipmentsPage() { if (!FEATURES.USE_REAL_DATA) { return <ShipmentsPageMock /> } return <ShipmentsPageReal /> } ` **Rollback Steps:** 1. Set feature flag to `false` in environment variables 2. Redeploy application 3. Verify mock version loads 4. Fix issues in real version 5. Re-enable feature flag **Estimated Rollback Time:** 5-10 minutes ### Database Rollback **Strategy:** Database migrations with rollback scripts **Implementation:** `sql -- migrations/001_initial_schema_up.sql CREATE TABLE shipments (...); -- ... rest of schema -- migrations/001_initial_schema_down.sql DROP TABLE IF EXISTS shipments CASCADE; -- ... drop all tables ` **Rollback Steps:** `bash # Using Supabase CLI supabase db reset # Or manually psql $DATABASE_URL -f migrations/001_initial_schema_down.sql ` **Estimated Rollback Time:** 2-5 minutes **⚠️ WARNING:** This will delete all data. Only use in development/staging. ### Full Application Rollback **Strategy:** Git-based rollback with Vercel **Rollback Steps:** 1. Identify last known good commit `bash git log --oneline # Find commit hash of working version ` 2. Revert to that commit `bash git revert HEAD~3..HEAD # Revert last 3 commits # Or git reset --hard abc123 # Hard reset to specific commit ` 3. Force push (if necessary) `bash git push origin main --force ` 4. Redeploy via Vercel `bash vercel --prod ` 5. Verify rollback successful - Check application loads - Test critical functionality - Review error logs **Estimated Rollback Time:** 10-15 minutes ### Data Recovery **Strategy:** Automated backups + Point-in-time recovery **Supabase Backups:** - Automatic daily backups (Pro plan) - Point-in-time recovery (last 7 days) **Recovery Steps:** 1. Go to Supabase Dashboard 2. Navigate to Database → Backups 3. Select backup point 4. Click "Restore" 5. Confirm restoration **Estimated Recovery Time:** 15-30 minutes (depends on database size) **Alternative: Manual Backup** ```bash # Create backup before migration pg_dump $DATABASE_URL > backup*$(date +%Y%m%d).sql # Restore if needed psql $DATABASE*URL < backup_20250115.sql ``` --- ## Post-Migration Checklist ### Functionality Verification - [ ] **Authentication** - [ ] Users can log in - [ ] Users can log out - [ ] Password reset works - [ ] Session persists across refreshes - [ ] Protected routes are actually protected - [ ] **Shipments** - [ ] Can create new shipment - [ ] Can edit shipment - [ ] Can delete shipment - [ ] Can view shipment details - [ ] Can filter shipments - [ ] Can search shipments - [ ] Status updates work - [ ] Event timeline shows correctly - [ ] **Customers** - [ ] Can create customer - [ ] Can edit customer - [ ] GSTIN validation works - [ ] Credit limit enforced - [ ] Can view customer history - [ ] **Invoices** - [ ] Can create invoice - [ ] Can generate PDF - [ ] PDF contains correct data - [ ] GST calculated correctly - [ ] Can email invoice (if implemented) - [ ] Payment tracking works - [ ] **Barcode & Scanning** - [ ] Barcodes generate correctly - [ ] QR codes generate correctly - [ ] Camera access works - [ ] Scanning updates shipment - [ ] Batch scanning works - [ ] **Dashboard** - [ ] Metrics load correctly - [ ] Charts display real data - [ ] Recent shipments show - [ ] Performance is acceptable (<3s load) ### Performance Verification - [ ] **Frontend** - [ ] Lighthouse score >90 - [ ] First Contentful Paint <2s - [ ] Largest Contentful Paint <2.5s - [ ] Time to Interactive <3s - [ ] No layout shifts (CLS <0.1) - [ ] **Backend** - [ ] API response times <200ms (p95) - [ ] Database queries <50ms (p95) - [ ] PDF generation <5s - [ ] Rate limiting works - [ ] No memory leaks - [ ] **Database** - [ ] All indexes created - [ ] RLS policies working - [ ] No slow queries (>100ms) - [ ] Connection pooling configured ### Security Verification - [ ] **Authentication & Authorization** - [ ] JWT signatures verified - [ ] CSRF protection enabled - [ ] RLS policies prevent data leaks - [ ] Cannot access other org's data - [ ] Role-based access working - [ ] **Input Validation** - [ ] All inputs validated (Zod) - [ ] SQL injection prevented - [ ] XSS attacks prevented - [ ] File upload restrictions work - [ ] **Security Headers** - [ ] HSTS enabled - [ ] X-Frame-Options set - [ ] CSP configured - [ ] X-Content-Type-Options set ### Testing Verification - [ ] **Unit Tests** - [ ] All tests passing - [ ] Coverage >70% - [ ] Critical paths covered - [ ] **Integration Tests** - [ ] All tests passing - [ ] Key flows covered - [ ] Database tests working - [ ] **E2E Tests** - [ ] All tests passing - [ ] Critical journeys covered - [ ] Tests run in CI/CD ### Monitoring & Observability - [ ] **Error Tracking** - [ ] Sentry configured - [ ] Errors being logged - [ ] Alerts set up - [ ] **Application Monitoring** - [ ] Vercel Analytics enabled - [ ] Speed Insights working - [ ] Uptime monitoring active - [ ] **Database Monitoring** - [ ] Slow query log enabled - [ ] Connection pool monitored - [ ] Disk usage tracked ### Documentation - [ ] **Technical Documentation** - [ ] API documentation complete - [ ] Database schema documented - [ ] Architecture diagrams updated - [ ] Deployment guide written - [ ] **User Documentation** - [ ] User guide created - [ ] Admin guide created - [ ] FAQ written - [ ] Video tutorials (optional) ### Deployment - [ ] **Environment Setup** - [ ] Production environment configured - [ ] Staging environment configured - [ ] Environment variables set - [ ] Secrets properly stored - [ ] **CI/CD** - [ ] Pipeline configured - [ ] Tests run automatically - [ ] Deployment automated - [ ] Rollback procedure tested - [ ] **Backups** - [ ] Automated backups enabled - [ ] Backup restoration tested - [ ] Backup retention configured ### Communication - [ ] **Stakeholder Communication** - [ ] Migration timeline shared - [ ] Key stakeholders informed - [ ] Training sessions conducted (if needed) - [ ] Support channels established - [ ] **User Communication** - [ ] Release notes prepared - [ ] Users notified of changes - [ ] Downtime communicated (if any) - [ ] Feedback channel set up --- ## Risk Management ### High-Risk Areas 1. **Authentication Migration** - **Risk:** Users locked out - **Mitigation:** Keep dev bypass flag, thorough testing - **Rollback:** Feature flag to disable auth 2. **Database Migration** - **Risk:** Data loss - **Mitigation:** Multiple backups, test on staging first - **Rollback:** Restore from backup 3. **PDF Generation** - **Risk:** Puppeteer crashes, memory issues - **Mitigation:** Resource limits, error handling - **Rollback:** Disable PDF generation, manual process 4. **Barcode Scanning** - **Risk:** Camera access denied, scanning fails - **Mitigation:** Fallback to manual entry - **Rollback:** Disable camera, manual only ### Risk Mitigation Strategies 1. **Incremental Rollout** - Deploy to staging first - Test thoroughly - Deploy to 10% of users (if possible) - Monitor for issues - Full rollout if successful 2. **Feature Flags** - All new features behind flags - Can disable instantly - No code deployment needed 3. **Monitoring** - Real-time error tracking - Performance monitoring - Alert on anomalies - 24/7 on-call during migration 4. **Communication** - Status page for users - Slack channel for team - Regular updates - Clear escalation path --- ## Timeline Summary | Week | Focus | Risk | Dependencies | |------|-------|------|--------------| | 1 | Database + Auth | High | None | | 2 | Core APIs | Medium | Week 1 | | 3 | Shipment Migration | Medium | Week 2 | | 4 | Invoice + PDF | High | Week 3 | | 5 | Barcode + Scanning | High | Week 4 | | 6 | Testing + Polish | Low | Week 5 | **Total Timeline:** 6-8 weeks (with 2-3 person team) **Critical Path:** 1. Database deployment 2. Authentication 3. Shipment APIs 4. Connect UI to APIs 5. PDF generation 6. Testing **Parallel Workstreams:** - Frontend migration (weeks 3-5) - Backend APIs (weeks 2-4) - Testing (ongoing) - Documentation (ongoing) --- ## Success Criteria ### Technical Metrics - ✅ 0% mock data remaining - ✅ 100% pages connected to database - ✅ >70% test coverage - ✅ <200ms API response time (p95) - ✅ >90 Lighthouse score - ✅ 0 critical security issues ### Business Metrics - ✅ Can create shipment end-to-end - ✅ Can generate and email invoice - ✅ Can scan and track shipments - ✅ <1% error rate - ✅ 99.9% uptime - ✅ Positive user feedback ### Team Metrics - ✅ All team members trained - ✅ Documentation complete - ✅ Runbooks created - ✅ On-call rotation established - ✅ Support process defined --- ## Conclusion This migration plan provides a structured approach to transforming TAC Cargo from a frontend prototype into a production-ready SaaS platform. By following this incremental strategy with proper testing, monitoring, and rollback procedures, we can minimize risks while delivering a robust logistics management system. **Key Takeaways:** 1. Migrate incrementally, one component at a time 2. Test thoroughly at each step 3. Always have a rollback plan 4. Use feature flags for safety 5. Monitor closely during migration 6. Communicate proactively with stakeholders **Next Steps:** 1. Review this plan with team 2. Set up development environment 3. Begin Week 1 tasks (database deployment) 4. Follow IMPLEMENTATION_GUIDE.md for detailed steps 5. Track progress against checklist --- **END OF MIGRATION PLAN** \_For architecture details, see: ARCHITECTURE_REVIEW.md* _For implementation steps, see: IMPLEMENTATION_GUIDE.md_ "
