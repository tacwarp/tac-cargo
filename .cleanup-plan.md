# Project Cleanup Plan - TAC Cargo

## Analysis Summary
- **Total Files:** 90,000+
- **Cleanup Target:** ~250+ MB
- **Safe to Delete:** Source maps, test files, redundant configs, duplicate docs

## Cleanup Categories

### 1. Source Maps (212.50 MB) - SAFE TO DELETE
- 16,286 `.map` files in node_modules
- Used only for debugging, not needed in production
- **Action:** Delete all .map files

### 2. Test Files & Snapshots (Estimate 10-20 MB)
- Test files: `*.test.ts`, `*.test.tsx`, `*.spec.ts`, `*.spec.tsx`
- Test snapshots: `*.snap` files (21 files)
- Mock data files
- **Action:** Remove all test-related files (keep test configs)

### 3. Redundant Documentation (5-6 MB)
- Multiple README files in node_modules (1,333 .md files)
- Changelog files
- License files duplicates
- **Action:** Remove node_modules docs, keep project-level docs

### 4. Unnecessary Config Files
- `.coderabbit.yml` or similar
- `.sonarqube/` directories
- Test coverage configs (`.nycrc` - 99 files)
- Multiple `.eslintrc` files (114 files in node_modules)
- **Action:** Remove tool-specific configs not in use

### 5. Build Artifacts & Cache
- `.tsbuildinfo` files (8 files, 0.96 MB)
- Coverage reports
- Temporary build files
- **Action:** Remove all build artifacts

### 6. Unused Dependencies (To Analyze)
- Check package.json for unused packages
- **Action:** Identify and remove after analysis

## Files to KEEP (Critical)
- All `/app` directory files
- All `/components` directory files
- All `/lib` directory files
- `/supabase/migrations/*.sql`
- Root config files: `next.config.ts`, `package.json`, `tsconfig.json`, etc.
- Project documentation: `SETUP_GUIDE.md`, `DEPLOYMENT_CHECKLIST.md`, etc.
- `/public` directory assets
- `.env.local` and `.env.example`

## Execution Order
1. ✓ Analyze and document (this file)
2. Remove source maps from node_modules
3. Remove test files and snapshots
4. Remove unnecessary config files
5. Remove build artifacts
6. Clean redundant node_modules documentation
7. Audit dependencies (manual review needed)
8. Test build after cleanup
9. Verify disk space freed

## Safety Measures
- Never delete files in: `/app`, `/components`, `/lib`, `/supabase`, `/public`
- Always test build after cleanup: `npm run build`
- Keep backup of package.json before dependency changes
- Use `-WhatIf` flag in PowerShell to preview deletions

## Expected Space Savings
- Source maps: ~212 MB
- Test files: ~15 MB
- Documentation: ~5 MB
- Build artifacts: ~5 MB
- Configs: ~2 MB
- **Total Expected:** ~240 MB freed
